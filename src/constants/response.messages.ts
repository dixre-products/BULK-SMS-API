const LoginSuccess = 'LOGIN.SUCCESS';
const InvalidCredential = 'INVALID.LOGIN.CREDENTIALS';
const InvalidPassword = 'INVALID.PASSWORD';
const SignUpSuccess = 'SIGNUP.SUCCESS';
const UserExist = 'USER.EXIST';
const UnAuthorizedRequest = 'UNAUTHORIZED.REQUEST';
const AccountNotVerified = 'ACCOUNT.NOT.VERIFIED';
const ServerError = 'SERVER.ERROR';
const NotFound = 'NOT.FOUND';

// input validation messages
const InvalidPhoneNumber = 'INVALID.PHONE';
const PasswordStrength = 'PASSWORD.STRENGTH.WEEK';
const InvalidInput = 'INVALID.INPUT';
const UserSuspended = 'USER.SUSPENDED';
const PinNotTimeOut = 'PIN.NOT.TIMEOUT';
const MAX_TRIALS_EXCEEDED = 'MAX.TRIALS.EXCEEDED';

const UserNotFound = 'USER.NOT.FOUND';

export default {
  UserNotFound,
  LoginSuccess,
  InvalidCredential,
  InvalidPassword,
  SignUpSuccess,
  UserExist,
  InvalidInput,
  UnAuthorizedRequest,
  AccountNotVerified,
  ServerError,
  NotFound,
  InvalidPhoneNumber,
  PasswordStrength,
  UserSuspended,
  PinNotTimeOut,
  MAX_TRIALS_EXCEEDED,
};
